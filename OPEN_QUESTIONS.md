# Open questions

2026-09-17 作者审阅后更新。**当前没有必须向作者追问才能完成计划的问题。** 作者已确认的决定见DECISIONS；下面是后续session的执行待办，不能重新包装成权限问题。

## Q1 — 已由M0收敛为最小来源问题（不阻塞）

- 已解决：`J_j^i`映射、三类gradient、两种分支规则及等价条件见`audit/implementation/M0.md`。T4.3页面顺序是`J_1^1,J_1^2,J_2^1,J_2^2`，必须重排；legacy脚本按旧代码顺序归属中间两项，(b)另有`b`差异。纸面(b)在`x0=(0,0)`为RHS `(0,0)`，实际`test6_4_1`为`(15,0)`，因此实际非静止与源码一致。
- 最小未解：仓库没有带版本记录的Fig4.3生成入口，不能唯一确认三个子图各自来自现有legacy还是另一未留存参数版。
- 处置结果：P1前三组采用明确标为新构造的参数族，不声称精确复现Fig4.3；trap单独采用来源明确的实际legacy版本。纸面/legacy差异继续由M0保存。
- 是否阻碍：否；不阻塞S1/T1/I1/X1，只限制P1的来源标签。

## Q2 — trap参数与规则记载（已由P1关闭）

- 已完成：`audit/implementation/P1.md`保存实际`A/b/x0/alpha`、scaled-own-gradient选择、与最近BR不全局等价的原因，以及内部`0.020<1.819`双收益下降见证。
- 为什么重要：可重复性需要参数与执行规则成对保存。
- 当前证据：作者确认使用实际仿真参数、图注可能笔误，不影响原研究内容；ACC Fig1亦表达相同trap概念。旧helper是scaled-gradient选择，纸面是BR距离选择。
- 可选方案：忠实复现实际helper；额外比较另一规则作为非必需补充。
- 数值层级：逐时弱性质为有限网格证据，不声称连续时间证明；独立`dt=.001`与MATLAB R2025b `dt=.002`均通过，分别给出相邻的内部见证。
- 是否阻碍当前阶段：否；科研定义、参数与见证已关闭。

## Q3 — 可实现性条件（作者解释与I1实现均已关闭）

- 问题：将“某些条件下不可能”和“满足定理保证成立”准确呈现。
- 为什么重要：必要/充分条件及特定构造失败不可混为普遍不可实现。
- 当前证据：作者已澄清，ACC(20)、Assumption1、Theorem2及(32)–(38)提供对应。
- 可选方案：以定理条件表及正例为主；增加必要条件失败的说明性对照。
- I1结果：已保留条件化叙述和两组数值正例；锚定、完整预算、凹性、general position及分支det均核查。完整最小不变域包含未证明，强`D(U,x0) subset D_bud`证书在当前`Q=0`参数下有反例点，因此未调用完整定理保证，也未写成普遍不可实现。
- 后续若需要正式套用定理，应另构造/证明`N_x0`包含或使用非零`Q`改变区域；这属于新任务，不是I1漏项。
- 是否阻碍当前阶段：否。

## Q4 — 缺原图脚本与case重建（S1/S2已关闭）

- S1状态：**已关闭五case重建部分**。本地case脚本仅作候选参考；S1采用新参数，已按Remark3.9斜率序、全部分支矩阵、eigenstructure、active cone与Nash几何确认Case1–5身份。
- 运行状态：已定位`/Applications/MATLAB_R2025b.app/bin/matlab`并以R2025b Update 7干净批处理执行；两张图、两张CSV及MAT均生成并核查。无S1复跑待办。
- S2状态：**已关闭**。3.6五例使用新截距形成Assumption 4秩零Nash点，并按20个分支determinant与Theorem3.11确认四稳定/一不稳定；3.7使用三组新参数表达one-saddle funnel、three-saddle funnel与opposite-wing三类主要noncompact构型。
- 为什么重要：必须覆盖不同case及主要数学内容，不以缺源省略。
- 当前证据：作者授权AI补足，不要求原图一致；3.5优先，3.7必做但较后。
- 可选方案：核查后复用参考参数；自行构造满足对应条件的新参数。
- 证据边界：`cdc2023`仍只作候选参考；正文`(c)/(e)`指代不一致不靠文件名解决。noncompact由Assumption1下mixed determinant signs及Proposition3.1确认，finite-window图和recession角采样只作展示。
- 是否阻碍当前阶段：否；S1/S2均完成且MATLAB R2025b与独立核查通过，原图参数provenance未恢复但不影响授权的新构造交付。

## Q5 — 五case在网站的具体呈现（R1）

- 问题：五case总览是正文、附图还是简要提及尚未定版。
- 为什么重要：不稳定case可说明数学结论和变换方法适用边界，但不应挤掉核心图组。
- 当前证据：作者认可原候选，并建议可以提及两agent五case及eigenvector分析。
- 可选方案：紧凑五case总览＋解释；正文只展示代表，其余链接到仓库完整图。
- 推荐：仓库保留全部，网站先按简要总览/附图候选；R1拿实际图比较后再定，不影响实现。
- 是否阻碍当前阶段：否；没有发布授权，也不需要现在做终选。

## X1 — 已完成；保留两处最小纸面差异

- 已完成：紫色Nash域和八个周围域到实际象限子集/轴/原点、四条代表纤维到轴点、三种轨迹、A到B分离区段、论文给定证书四分支裕量、Lyapunov等高线和原网格拉回均见`audit/implementation/X1.md`；MATLAB R2025b与独立RK4核查均通过。
- 纸面差异1：按论文展示参数和`x0=(6.5,-10)`直接计算为`eta(x0)=(-2.5,-13.25)`，正文写`(-2.5,-13.5)`。实现使用前者，未静默替换。
- 纸面差异2：Figure 6标注的A到B轴段上，MATLAB和独立核查均得到像范数从约`8.30`降到`2.33`；正文写`||eta(x_A)||<||eta(x_B)||`，不等号方向与图/计算相反。
- 处置：两处均不影响(58)–(60)证书、三轨迹分离事实或稳定性充分条件；作为最小来源问题保留，不升级为研究结论错误，也不阻塞T1/S2/R1。

## T1 — 已完成；paper/legacy版本已分开

- 已完成：cube全部6面/12边/8顶点、triangular prism全部5面/9边/6顶点，以及生产/污染应用入口和非二次示意，见`audit/implementation/T1.md`。
- 版本处置：cube paper使用`-.6/-.5`而legacy为`+.6/+.5`，且legacy固定`M`只加权`b`；prism paper的agent 2首系数为`-1`而legacy为`+1`。T1运行paper字面版并同时加权`M/b`，没有静默混合。
- 证据边界：Examples2.9/2.10正文给出微分同胚结论；MATLAB有限网格验证实现中的可逆性、非退化Jacobian和方程残差，但不替代定理证明。非二次图无完整函数，故只保留示意身份。
- 是否阻碍：否；无T1遗留问题，不阻塞S2/R1。
