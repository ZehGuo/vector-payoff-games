# 后续 session 执行计划

版本：2026-09-18；M0、S1、P1、I1、X1已完成各自记录的实现与独立核查；S1、P1、I1、X1均已通过MATLAB R2025b复跑；其余任务未实施。长期范围见 PROJECT_BRIEF.md，最新符号/分支契约见 audit/implementation/M0.md，完整图目录见 audit/FIGURE_MANIFEST.md。**旧提交2ab0d68的Q1计算不可作为正确基线。**

## 1. 要重建的问题

研究对象是多agent向量收益非合作系统。仓库应连贯回答：

1. 多目标使Nash解形成什么集合？权重单纯形的实体、面、边如何映射？
2. 选定pseudo-gradient规则后，不同BR/eigenvector构型为什么稳定或不稳定？两agent五种case的区别是什么？
3. 非双射坐标变换如何帮助稳定性分析？原轨迹的像为何不等于独立变换系统轨迹？哪些结论受假设限制？
4. 自身方向的收益改善为何不等于总收益改善？全目标改进、弱改进和非弱改进如何区分？弱改进为何仍可出现trap？
5. 怎样以部分收益激励改变行为，在给定条件下达到弱改进、无trap和预算要求？target、omega、sigma和初值可行域分别起什么作用？

不要求像素、参数或原图布局一致；要求每组案例的数学身份、比较意义和证据明确。原始代码、原型均是参考，不能决定新实现结构。只在必要时设计最小内部组织，不预先设计公共API。

## 2. 范围、优先级与统一交付

| 任务 | 优先级 | 核心产物 | 依赖 | 状态 |
|---|---|---|---|---|
| M0 符号与动力学契约 | 首先 | 正确agent/objective映射、RHS选择规则、Q1对应核查 | 无 | 已完成（静态核查；见audit/implementation/M0.md） |
| S1 两agent五case | 高；作者特别强调3.5 | 3.5五case完整比较与eigenvector证据 | M0公共约定；不必等Q1整项关闭 | 已完成（MATLAB R2025b＋独立核查通过，见S1.md） |
| P1 收益性质与trap | 高 | nonweak/all/weak三组、独立trap图 | M0；Q1限制Fig4.3纸面/legacy来源标签 | 已完成（前三组精确代数；trap实际规则有限采样；MATLAB R2025b复跑通过，见P1.md） |
| I1 激励与预算 | 高 | 设计几何、sigma/omega、激励前后收益及预算 | M0；可复用P1诊断 | 已完成（MATLAB R2025b＋独立核查通过；定理不变域包含未证明，见I1.md） |
| X1 变换与稳定性 | 高 | 原/像/独立轨迹与Lyapunov、适用边界 | M0；S1提供case解释 | 已完成（MATLAB R2025b＋独立核查通过；见X1.md） |
| T1 拓扑与应用 | 中 | cube/prism全部面对应、生产环境例 | agent/objective记法确认即可 | 待执行 |
| S2 退化与非紧case | 中后；3.7必须保留 | 3.6五case及3.7非紧代表构型 | S1 | 待执行 |
| R1 整合与展示评审 | 最后 | 全范围覆盖报告、简洁文档、候选图包 | 以上科研任务完成 | 待执行 |

下一session推荐按 T1 → S2 → R1 逐项执行；X1、S1、P1、I1已完成并停止。依赖已列出，可调整顺序；这里不自动创建session，也不启用subagent。不同时在同一checkout运行多个改文件任务。

每个任务统一交付：

- 一个可从干净MATLAB工作区运行的入口；实际路径由实施session记录，不预先冻结API。
- 独立参数记录：agent/objective/状态顺序、完整A/b/c、初值、alpha、分支规则、时间和求解设置、来源及与纸面差异。
- 数值结果与图；关键缩放、轴、图例、比较组清楚。结果保存位置和Git忽略策略注明，不把原始PDF/快照意外提交。
- `audit/implementation/<任务ID>.md`：问题、选择、证据、运行环境、验收结果、限制和未解问题；图/数据的实际路径。
- 更新 PROJECT_STATE、DECISIONS、OPEN_QUESTIONS、本计划状态及manifest的对应运行状态；本地提交，只提交该任务文件，不推送。

验证必须对应科研性质：有限网格可提供反例/观察，不能作连续时间证明；公式恒等式、定理假设、数值求解和图覆盖分别记录。不要求重建证明，也不以单元测试通过宣称论文内容齐全。

## 3. 任务卡

### M0 — 符号、梯度与实际执行规则

**读入**：audit/AUTHOR_REVIEW §Q1–2；T p26–31、p55及Fig4.3；ACC p2式(7)；L cdc2024/test6_4_1、test10_1及testfun2；G trap及同目录helpers；E规则(27)/(28)。

**工作**：

1. 建立 `J_j^i ↔ agent i / objective j ↔ 代码变量` 对照，逐页视觉核对，不能使用PDF文本出现顺序。T4.3三个例子的中间两项尤其要核对。
2. 明确完整gradient、own-gradient、pseudo-gradient及外部性/总收益导数；写出最近BR和scaled-gradient两个选择规则、等价条件、inactive/tie规则。
3. 对Q1分别记录纸面字面参数和实际脚本参数，在初值输出四个完整梯度、own分量、BR距离、选中分支和RHS，再观察第一步。纠正先前错误，解释差异所在；不可把条件性的字面零值推断成作者实际仿真停止。
4. 把历史JSON失效部分与有效legacy数值分清；是否补最小MATLAB公式核查由任务判断，不调用旧原型作参考答案。

**验收**：每个量能从一条纸面公式/一处实际源码追溯；没有混淆上下标；两个规则明确命名。Q1若仍有未确认的纸面对应，记录最小差异，允许用具有正确性质且来源明确的仿真案例继续，不冻结其他任务。此时不把“复现纸面参数”标为通过。

**停止点**：形成 `audit/implementation/M0.md` 和可复用的定义说明；不顺便重构全部代码。

### S1 — 四稳定、一不稳定的五种case（优先3.5）

**读入**：T Lemma3.6、Remarks3.7–3.9、Theorem3.10、Fig3.3/3.5；L ifac2023/case1–5作为候选参考，不能假定原图精确对应。

**工作**：建立与论文Case1–5对应的斜率排序/符号条件；构造满足相应假设的五套参数。文件名case3可能对应论文不稳定Case5，必须以数学条件重新命名。对四个双active分支给出矩阵、det、eigenvalues、real eigenvectors与BR锥关系；非实特征值时如实记为旋转，不虚构实特征向量。记录0/1/2目标域转移区别。选择有解释力的多初值轨迹和向量场，所有case用可比较图例。

**产物**：五case总览、参数及判据表、至少一幅eigenvector/域转移解释图。保存所有五例，不只展示漂亮的四个稳定例。

**验收**：case身份来自斜率/矩阵/域条件；稳定与不稳定不能只靠末状态判断；不稳定例有对应不稳定方向/轨迹见证。结论附论文假设，不外推到未覆盖的混合det情形。网站附图文案说明坐标变换用于分析，不会消灭真实不稳定性；不宣称不稳定例等于变换方法“错误”。

### P1 — 三种收益性质与trap

**读入**：T Chapter4、Fig4.1–4.3/5.1；ACC Definitions2–3/Fig1；cdc2024和G trap；M0说明。

**工作**：三组分别满足nonweak、all-payoff nondecreasing、weak而非all。每组相图对应四目标时间曲线/导数；Γ与Ω明确区分。另一组trap使用作者确认的实际仿真参数及helper行为，展示某时间对两项收益同时变差，而每时刻至少一个目标不降。ACC与thesis图形不同，不强行追求相同t1/t2。

**验收**：nonweak须有同一agent两目标负导数见证；all/weak须报告有限区间观测及可引用的充分条件；trap有显式t1<t2及收益差表，不能仅看首尾。不得把trap替换nonweak列。不得因图注不同而自行换掉已确认的仿真定义。

### I1 — 激励可行性、参数作用及前后比较

**读入**：T5.2–5.6、Theorem5.4；ACC(20)–(38)/Theorem2/Fig2–3；G main/test3_*/test5_*。原型计算只作参考，特别检查budget及general-position检验缺口。

**工作**：

- 明确只改Ji1、目标U及target、初值锚定、aggregate预算身份；区分必要条件不满足、充分条件未证和可实现设计。
- INC-0：社会Pareto vs Nash、初值水平域/预算域/target、原/改BR与轨迹、原/改收益、总transfer。
- sigma图保留thesis三个边界区间；加入ACC Fig3的两个budget补集及“所有sigma都排除的区域”比较，不声称ACC参数与旧slider同源。
- omega机制用同游戏控制其他量的两个配置解释target位置；INC-ω仍是另一游戏的补充实验，不能当单因素对照。
- 修正test5_3遗漏omega的budget，明示各缩放；Q/Qi=0为核心，非零Q仅保留解释/扩展，不需额外做全扫描。

**验收**：pi(x0)=0、预算完整identity、完整general position/own凹性/分支det条件记录；有条件引用定理，若尚无不变域包含证明只报告验证层级。原/改收益都可见，预算不只末时刻，target不等于有限时间终点。保持“满足theorem则保证”的作者解释，不写成一般不可激励。

### X1 — 坍缩变换、独立动力学与证书

**读入**：E2–6、(27)/(28)/(50)/(58)–(62)；L ifac_extention及ecc2026（参数分开）；P transformedBranch和verifyLyapunovCertificate仅供审查。

**工作**：原空间域→象限/轴/原点图；分别得到x(t)、η(x(t))、独立积分tilde x(t)，并标识分离区段。不得通过插值η(x(t))伪装独立解，也不得在无唯一逆的轴上返回任意点。展示给定T的四分支严格证书裕量、原网格上的V(η(x))拉回。先验算给定证书，再按需要求新证书；缺testfun3需依据论文补足，不靠工作区残留。

**验收**：三种轨迹来源独立标清；branch一致性/轴/tie行为记录；U/W逐元素非负与严格裕量检查；范数暂增、稳定性和Lyapunov下降不能混称。与S1联系说明方法假设和非双射信息损失，不拿“求解器没找到证书”作为不稳定证明。

### T1 — 参数域、面映射和应用入口

**读入**：T2.1–2.6、Examples2.8–2.11；L acc2024 cube/prism；原型own-row补全风险。

**工作/验收**：同时加权M与b，保留cube全部6面/prism全部5面及颜色/边对应；权重域与状态像并列，说明满射与双射的不同。完整参数/符号版本可选择但须记录。集中式Pareto集不能称Nash。生产/污染例依纸面公式作为应用入口；非二次示意保持“示意”而非无来源数值图。

### S2 — 秩退化与noncompact补充

**读入**：T Assumption4/Theorem3.11、Fig3.4/3.6/3.7；cdc2023作为候选参考；S1输出。

**工作/验收**：3.6保留五case在秩退化几何下的区别；3.7构造能表达原图主要不同构型的非紧例，不必复用原参数，数学解释、BR、Nash和代表轨迹齐备。检验缺少compact假设后哪些结论不能套用；不把有限坐标窗口内“看起来封闭”当作紧性证明。优先级较后，但不得从交付清单删除。

### R1 — 集成、文档与网站候选评审

**工作/验收**：从干净会话运行各入口；对manifest每项给出实际输出或明确的示意/讨论性归档；核对参数、来源、科学结论、图例缩放、运行环境与许可。选择作者已认可的候选组，五case用紧凑总览或附图解释；新的重要选图变动另列建议。仓库完整范围与网站精选不能混淆。只做本地可审阅结果，不推送/公开发布。

## 4. 交给新 session 的可复制指令

将下面的“任务ID”替换为表中任一项即可：

> 在 `/Users/lei/ZehuiGuo/vector-payoff-games-rebuild` 执行 IMPLEMENTATION_PLAN.md 中的任务ID。先读 PROJECT_BRIEF.md、PROJECT_STATE.md、DECISIONS.md、OPEN_QUESTIONS.md、audit/AUTHOR_REVIEW.md，再读该任务指定材料。只实施该任务及必要依赖，不把previous_prototype当正确基线，不改原始材料，不推送或发布。原图参数和布局不要求一致，但须保留case区别与数学比较。注意2ab0d68的Q1上下标转录已撤回。完成任务卡的交付与验收，更新audit/implementation/任务ID.md和全部相关记忆/清单，记录剩余问题及下一任务，创建本地commit后停止。普通不确定性采用有依据的判断并记录；不要因缺原图代码停止。

若本session缺MATLAB连接或运行权限，先完成仍可做的公式/实现/静态审查，明确“MATLAB待验证”，不得伪报通过；不要绕过已拒绝的启动许可。只有真正改变科研范围、无法兼容定义或公开发布等边界才提问，提问前完成独立工作。
